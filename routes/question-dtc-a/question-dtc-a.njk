{% extends "base.njk" %}

{% block content %}

    <h1>{{ __('dtc.title') }}</h1>
    <div>
        {% call form() %}
            <input type="hidden"
                  name="_csrf"
                  value="{{ csrfToken }}">

            {{ radioButtons('dtc_a',
                [
                    { value: 'yes', text:__('dtc-a.yes') },
                    { value: 'no', text:__('dtc-a.no') }
                ],
                data.dtc_a,
                __('form.dtc-a'),
                errors,
                {
                    required: true,
                    bullets: [__('dtc.bullet1'), __('dtc.bullet2'), __('dtc.bullet3')]
                }
            )}}
            {{ formButtons() }}
        {% endcall %}
    </div>

{% endblock %}
